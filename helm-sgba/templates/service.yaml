apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-sgba.fullname" . }}
  labels:
    {{- include "helm-sgba.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-sgba.selectorLabels" . | nindent 4 }}
